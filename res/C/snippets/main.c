int main() {

    int test;
    test = 0;

    test < 4;

    test++;

    int a = 0;
    int aa = test;
    
    if (a < b) {
        test;
    }

    if (x < y) {
        test;
    } else {
        test;
    }

    for (test = 0; test < 4; test++) {
        test;
        for (test = 0; test < 4; test++) {
            test;
        }
    }

    while (test < 4) {
        test++;
    }

    switch (expression) {
        case x:
            test++;
            break;
        case y:
            break;
            test++;
        default:
            break;
    }

    int y = a < b ? 1 : 0;

    return 10;
}